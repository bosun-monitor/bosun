language: go
sudo: false
go:
  - tip

notifications:
  slack: bosun:2Z0edJEDfXMDnhg94uLNKzNj

install:
  - rsync -az ${TRAVIS_BUILD_DIR}/ $HOME/gopath/src/bosun.org/

script:
  - go fmt bosun.org/... | grep . && echo go fmt not run && exit 1 || exit 0
  - go test bosun.org/...
